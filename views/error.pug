extends layout

block content
  article.portfolio-intro
    h1= `There's been a problem`
    h2= `Error status code: ${err.status} - ${err.title}`
    p.error-p= `${err.message}`

    a.btn-link(href=`/`) Back to home page

    div.error-stack
      hr
      p The message below is for development purposes only
      pre #{err.stack}
